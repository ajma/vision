using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace Vision.Models
{
    public enum Gender
    {
        Unspecified = 'U',
        Male = 'M',
        Female = 'F',
        Neutral = 'N'
    }

    public enum GlassesSize
    {
        Unspecified = 'U',
        Small = 'S',
        Medium = 'M',
        Large = 'L'
    }
}